from setuptools import setup, find_packages

setup(
    name='graph-visualizer-api',
    version='1.0.0',
    description='API library with abstractions for Graph Structure Visualizer',
    packages=find_packages(),
    python_requires='>=3.8',
)
